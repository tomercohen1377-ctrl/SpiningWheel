<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpiningWheel">

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          SpinActivity — full-screen Compose animation activity.
          Launched by SpinActionCallback via the ACTION_SPIN intent action
          when the user taps the spin button on the home-screen widget.

          android:launchMode="singleTop" prevents stacking multiple instances
          if the user taps the widget rapidly.
          android:excludeFromRecents hides it from the task switcher.
        -->
        <activity
            android:name=".SpinActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SpinWheel.FullScreen"
            tools:ignore="AppLinkUrlError">
            <intent-filter>
                <action android:name="com.example.spinwheel.ACTION_SPIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
          GlanceAppWidgetReceiver — must be declared in the app manifest so the
          Android launcher discovers the widget at install time.
        -->
        <receiver
            android:name="com.example.spinwheel.SpinWheelWidgetReceiver"
            android:label="Spin Wheel"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/spin_wheel_widget_info" />
        </receiver>

    </application>

</manifest>
