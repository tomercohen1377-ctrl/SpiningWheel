{
  "name": "react-native-spinwheel",
  "version": "1.0.5",
  "description": "Android home-screen Spin Wheel widget for React Native. Self-contained SDK: JS bridge + Glance widget UI + asset pipeline (use bridge URLs, no Firebase).",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/",
    "android/",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "pack": "npm run build && npm pack"
  },
  "keywords": [
    "react-native",
    "android",
    "widget",
    "home-screen",
    "spin-wheel",
    "kotlin",
    "glance",
    "sdk"
  ],
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-native": "^0.72.0",
    "typescript": "^5.0.0"
  }
}
